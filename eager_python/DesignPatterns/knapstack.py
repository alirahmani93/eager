def knapstack(capacity , object: list):
    bag =[]
    density = {}
    for weight_val in object:
    density = dict(density.items(),key = lambda item: item[1],reversed=True))

    while capacity
    
    return bag

#          W,V
objects = [(6,5),(2,3)]
# objects= {'a':[6,5],'b':[2,3]}

print(knapstack(10,objects))